# pss_campaigns

<div id="dy_${dyVariationId}" class="sale-banner">
  <div class="sale-banner__content">
    <span class="sale-banner__badge">${discountBadge}</span>
    <div class="sale-banner__text">
      <h2 class="sale-banner__headline">${saleHeadline}</h2>
      <p class="sale-banner__description">${saleDescription}</p>
    </div>
    <a href="${ctaUrl}" class="sale-banner__cta" aria-label="${ctaLabel}">${ctaLabel}</a>
  </div>
</div>
